# Minimal eclipse-mosquitto config for the HMI plugin's docker-compose dev
# setup: plain MQTT on 1883, MQTT over WebSocket on 9001, anonymous access
# (local dev only - do not use these settings for anything internet-facing).
listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
